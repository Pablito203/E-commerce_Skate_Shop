import { MonetarioDirective } from './monetario.directive';

describe('MonetarioDirective', () => {
  it('should create an instance', () => {
    const directive = new MonetarioDirective();
    expect(directive).toBeTruthy();
  });
});
